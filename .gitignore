# Local environment variables — never commit real passwords or secrets
.env
.env.local
.env.*.local

# Node (if tools are ever added)
node_modules/

# OS / editor files
.DS_Store
Thumbs.db
.vscode/
*.swp

# Local Netlify folder
.netlify

# Build output (Netlify builds from source)
dist/

# Local backups and editor copies
backup_*/
*.backup-*
*.rtf

# Analysis engine (DSA Adapter): vendored upstream clone and local venv are
# recreated from analysis-engine/README.md instructions; never commit them.
analysis-engine/vendor/
analysis-engine/.venv/
analysis-engine/__pycache__/
