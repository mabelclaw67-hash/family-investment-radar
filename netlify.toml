[build]
  publish = "."
  command = ""   # Static site — no build step needed

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  # Login password is defined in src/config.js.
