[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

# Required environment variable — set in Netlify UI → Site settings → Environment variables:
#   VITE_SITE_PASSWORD = <your real password>
# Do NOT commit the real password to source control.
